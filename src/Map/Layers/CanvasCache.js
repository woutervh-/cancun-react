import React from 'react';

export default class CanvasCache extends React.Component {
    static propTypes = {
        id: React.PropTypes.string.isRequired
    };

    render() {
        return null;
    }
};
